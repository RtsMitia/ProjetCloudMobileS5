package com.projet.lalana.repository;

import com.projet.lalana.model.SignalementHistory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SignalementHistoryRepository extends JpaRepository<SignalementHistory, Integer> {
}
